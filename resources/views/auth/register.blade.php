@extends('layouts.app') @section('title','Criar conta — Sutoorii Tickets') @section('content')<section class="auth-card"><h1>Criar conta</h1><p>Disponível apenas para e-mails @sutoorii.com.</p><form method="post" action="{{ route('register.store') }}">@csrf<label>Nome<input name="name" value="{{ old('name') }}" required></label><label>E-mail<input name="email" type="email" value="{{ old('email') }}" required></label><label>Senha<input name="password" type="password" required></label><label>Confirmar senha<input name="password_confirmation" type="password" required></label>@if($errors->any())<div class="error">{{ $errors->first() }}</div>@endif<button class="button full">Criar e validar e-mail</button></form></section>@endsection

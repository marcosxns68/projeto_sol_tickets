@extends('layouts.app') @section('content')<div class="page-head"><div><p class="eyebrow">MINHA CAIXA</p><h1>Tickets</h1></div><a class="button desktop" href="{{ route('tickets.create') }}">+ Novo ticket</a></div><div class="filters"><button>Todos</button><button>Responsável</button><button>Colaborador</button><button>Seguidor</button><button>Não atribuídos</button><button>Atrasados</button></div><section class="ticket-list">@forelse($tickets as $ticket)<a class="ticket" href="{{ route('tickets.show',$ticket) }}"><div class="ticket-top"><span class="number">#{{ $ticket->number }}</span><span class="status" style="--status:{{ $ticket->status->color }}">{{ $ticket->status->name }}</span></div><h2>{{ $ticket->title }}</h2><p>{{ Str::limit($ticket->description,110) }}</p><div class="meta"><span>{{ ucfirst($ticket->priority) }}</span><span>{{ $ticket->assignee?->name ?? 'Não atribuído' }}</span><span>{{ $ticket->due_at?->format('d/m/Y H:i') ?? 'Sem prazo' }}</span></div></a>@empty<div class="empty"><span>✓</span><h2>Sua caixa está vazia</h2><p>Crie um ticket para começar.</p></div>@endforelse</section>{{ $tickets->links() }}@endsection

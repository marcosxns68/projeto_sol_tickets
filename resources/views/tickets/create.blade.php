@extends('layouts.app') @section('title','Novo ticket') @section('content')<div class="page-head"><div><p class="eyebrow">NOVO</p><h1>Criar ticket</h1></div></div><form class="panel form" method="post" action="{{ route('tickets.store') }}">@csrf<label>Título<input name="title" value="{{ old('title') }}" maxlength="180" required placeholder="Resumo claro do que precisa ser feito"></label><label>Descrição<textarea name="description" rows="7" required placeholder="Explique os detalhes, contexto e resultado esperado">{{ old('description') }}</textarea></label><div class="grid"><label>Prioridade<select name="priority"><option value="low">Baixa</option><option value="normal" selected>Normal</option><option value="high">Alta</option><option value="urgent">Urgente</option></select></label><label>Prazo<input type="datetime-local" name="due_at" required></label><label>Departamento<select name="department_id"><option value="">Triagem / nenhum</option>@foreach($departments as $department)<option value="{{ $department->id }}">{{ $department->name }}</option>@endforeach</select></label></div>@if($errors->any())<div class="error">{{ $errors->first() }}</div>@endif<div class="actions"><a href="{{ route('dashboard') }}">Cancelar</a><button class="button">Criar ticket</button></div></form>@endsection
